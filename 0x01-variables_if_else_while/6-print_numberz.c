#include <stdio.h>
/**
 * main - this is the program's entry point
 * Return: 0 is always correct
 */
int main(void)
{
	int b;

	b = 0;
	while  
		(b < 10) {
			putchar(b + '0');
		}
	putchar('\n');
	return (0);
}
