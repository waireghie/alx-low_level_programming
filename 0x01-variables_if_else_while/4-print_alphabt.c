#include <stdio.h>
/**
 * main - this is the entry point
 * Return: when 0, means success
 */
int main(void)
{
	char c;
	c = 'a';
	while
		(c <= 'z') {
			if ((c != 'q' && c != 'e') && c <= 'z')
				putchar(c);
			c++;
			}
	putchar('\n');
	return (0);
}
